# Ignore everything by default
*

# But keep directories (so Git can traverse)
!*/

# Keep Rust source and common text/config files
!**/*.rs
!**/*.toml
!**/*.md
!**/Cargo.lock
!**/Cargo.toml

# Keep this file
!.gitignore

# (Optional) Always ignore Rust build output folders explicitly
**/target/

